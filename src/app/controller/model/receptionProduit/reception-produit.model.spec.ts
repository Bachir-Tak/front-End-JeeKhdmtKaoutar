import { ReceptionProduit } from './reception-produit.model';

describe('ReceptionProduit', () => {
  it('should create an instance', () => {
    expect(new ReceptionProduit()).toBeTruthy();
  });
});
